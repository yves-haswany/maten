from setuptools import setup, find_packages

setup(
    name="maten",  # Replace with your project name
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here, e.g. 'requests'
    ],
)
